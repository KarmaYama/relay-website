# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build cache manifests
*.tsbuildinfo

# System
.DS_Store

# Android builds. APKs are ~50MB of already-compressed, undeltifiable data that
# git keeps forever — they're published as GitHub Release assets instead, so the
# repo never carries them. GitHub also hard-rejects any single file over 100MB.
*.apk
*.aab
